# Usato solo dalla modalita' Online. Lascialo vuoto/assente per giocare in LAN
# (il client cerchera' automaticamente il server sullo stesso indirizzo, porta 3001).
# Per giocare con persone su reti diverse, imposta qui l'URL pubblico del server
# Socket.io una volta che l'hai messo online (es. su Render), poi rifai la build.
VITE_SERVER_URL=https://il-tuo-server.onrender.com
